.col-md-6
	%h2.text-center
		CV de
		= @user.email
	%iframe{:height => "780", :src => @user.display_link, :style => "border: none;", :width => "600"}


.col-md-5.col-md-offset-1
	%h2.text-center
		Contenu de la correction
	%br
	.panel.panel-default
		.panel-body
			= form_tag("/send_mail", method: "get") do
				.form-control
					= label_tag(:q, "Corrections")
				%textarea.form-control{:rows => "3"}
					= text_area_tag(:q, nil)
				= submit_tag "Envoyer la correction", class: "btn btn-default"

	%h3
		= link_to "Retourner à la liste des cvs", admins_path, {:style => 'color: #FFFFFF;'} 
